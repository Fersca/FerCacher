Jcached. - Last update: March 12th, 2012.

This is a super-simple Key-value cache written in plain java.
Put, Delete and Get elements in constant-time.

Version 0.1:

- Multi-Thread connections listening at port 5555.
- Store only strings (only for now)
- Implements a LRU Double-Linked Queue to delete old elements.

Help wanted!
- To improve synchronized, today synchronized methods are used but can be improved for sure.
- To Implements a thread pool for the connections.
- To download/restore the cache values to disk.
- To write a java client.
- 
...
- To etc.

@fersca
